import React from 'react'
import ReactDOM from 'react-dom/client'
import RouterComponent from './components/Router/Router.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
      <RouterComponent />
  </React.StrictMode>,
)
